name: wiz_app
description: Wiz foi feito para estar com você.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^6.0.0
  vibration: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/
